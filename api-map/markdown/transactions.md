# Transactions

Base: `https://api.squarespace.com/1.0/commerce`

## `GET /transactions` — list transactions
Query: `cursor`, `modifiedAfter` (ISO 8601), `modifiedBefore` (ISO 8601). Read-only.
`squarespace-cli transactions list`.
