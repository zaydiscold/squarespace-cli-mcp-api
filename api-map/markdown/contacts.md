# Profiles / Contacts

Base: `https://api.squarespace.com/1.0/commerce`

## `GET /profiles` — list profiles (contacts)
Query: `cursor`, `filter`, `sortDirection` (`ASC` | `DESC`), `sortField`. Read-only.

## `GET /profiles/{profileId}` — get one profile
Read-only.
