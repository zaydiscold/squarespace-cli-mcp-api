# Release notes — 2026-06-22

## Rebrand: repo renamed to `squarespace-cli-mcp-api`

On **2026-06-22 (14:07 PDT)** the GitHub repository `zaydiscold/squarespace-cli` was renamed to
**`zaydiscold/squarespace-cli-mcp-api`**, and the README title changed to **"Squarespace CLI (MCP + API)"**.

This adopts the `<venue>-cli-mcp-api` naming convention now shared across the personal CLI repos
(matching `goodreads-cli-mcp-api` and `robinhood-cli-mcp-api`).

### What changed
- GitHub repository slug: `squarespace-cli` → `squarespace-cli-mcp-api` (GitHub auto-redirects the old URL).
- README H1 title and the GitHub description.

### What did NOT change
- The npm package and bin names (`@zaydiscold/squarespace-cli`) are unchanged, so installs, imports, and
  MCP registration are unaffected.
- No code, route-map, or behavior changes — branding and references only.
